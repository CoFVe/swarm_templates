version: '3.9'
services:
  mfsmaster:
    image: gerardogve/moosefs-master:latest
    deploy:
      placement:
        constraints:
          -  node.labels.nuc2==true
        preferences:
          - spread: node.labels.nuc2
    networks:
      - outside
    volumes:
      - /docker/mfsmaster:/var/lib/mfs
      - /docker/mfsmaster:/etc/mfs

networks:
  outside:
    external:
      name: "host"
